{% extends 'base.html.twig' %}

{% block title %}Nouvelle adresse{% endblock %}

{% block body %}

    <h1>Ajouter une adresse</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_adresse') }}">Retour</a>

{% endblock %}
